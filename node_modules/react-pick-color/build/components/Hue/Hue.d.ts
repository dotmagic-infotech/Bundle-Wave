import React from 'react';
import { HslColor } from '../../types';
type HueProps = {
    hsl: HslColor;
    onChange?: (color: HslColor) => void;
};
declare const _default: ({ hsl, onChange }: HueProps) => React.JSX.Element;
export default _default;
